{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Reset live server",
            "command": "kill -9 $(lsof -t -i:3000) $(lsof -t -i:3001)",
            "type": "shell",
            "problemMatcher": [
                "$tsc"
            ],
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}