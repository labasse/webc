# Web C(++)

A starting template for client page using C++ and JS.

## Building/Debugging from Codespace

Start a *Live Preview - External browser* session

Select *"Rebuild all"* in the *Run and Debug* left pane

C(++) debugging is in debug tab of the browser inspector